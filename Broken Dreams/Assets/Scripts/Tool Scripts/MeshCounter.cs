using UnityEngine;
using System.Collections;

public class MeshCounter : MonoBehaviour {

	// Use this for initialization
	void Start () {
       // float numTriangles = gameObject.GetComponent<MeshRenderer>() / 3;
     //   Debug.Log(gameObject.name + "has " + numTriangles + " polys");

    }
}
